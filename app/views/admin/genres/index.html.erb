<div>
    <div>
        <div>
            <h4>ジャンル一覧・追加</h4>
        </div>
        <div>
         ジャンル新規登録フォーム
        </div>
        <div> 
         <table>
             <thead>
                 <tr>
                     <th>ジャンル</th>
                     <th></th>
                 </tr>
             </thead>
             <tbody>
                 <% @genres.each do |genre| %>
                 <tr>
                     <td><%= genre.name %></td>
                     <td><%= link_to "編集する", edit_admin_gemre_path(genre.id) %></td>
                 </tr>
                 <% end %>
             </tbody>
            
         </table>
        </div>
    </div>
</div>