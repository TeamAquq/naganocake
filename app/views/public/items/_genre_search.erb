<div class="col-md-2">
  <table class="table">
    <thead>
      <tr>
        <th>ジャンル検索</th>
      </tr>
    </thead>
    <% Genre.all.each do |genre| %>
    <tbody>
     <tr>
       <td><%= link_to genre.name, genre_path(genre.id) %></td>
     </tr>
    </tbody>
    <% end %>
  </table>
</div>